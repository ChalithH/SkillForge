# SQL Server Profile Configuration
# Use this profile to demonstrate SQL Server compatibility
# 
# Usage:
#   1. Copy this to .env: cp .env.sqlserver .env
#   2. Start all services: docker-compose --profile sqlserver up
#   3. Access at http://localhost:3000 (frontend) and http://localhost:5000 (backend)

# Database Configuration - SQL Server
ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=SkillForge;User=sa;Password=LocalDev@123;TrustServerCertificate=true

# SQL Server Password (must match docker-compose.yml)
SA_PASSWORD=LocalDev@123

# JWT Configuration - DEVELOPMENT ONLY (never use in production)
JWT_SECRET_KEY=ETuUafWQHbW5SD8xw99bFgBXlaw6qrP15/5YNTpynLpcuW6F6PA140d0iBK0wto0idnm2KV/VvOMQrK4ONHUjg==

# Application will automatically detect SQL Server from connection string pattern
# and configure the appropriate Entity Framework provider