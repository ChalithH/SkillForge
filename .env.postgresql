# PostgreSQL Development Profile Configuration
# Use this profile for development with production parity
# 
# Usage:
#   1. Copy this to .env: cp .env.postgresql .env
#   2. Start all services: docker-compose up
#   3. Access at http://localhost:3000 (frontend) and http://localhost:5000 (backend)

# Database Configuration - PostgreSQL for Development
ConnectionStrings__DefaultConnection=Host=postgres-dev;Port=5432;Database=skillforge_dev;Username=skillforge_test;Password=test_password_123

# JWT Configuration - DEVELOPMENT ONLY (never use in production)
JwtSettings__SecretKey=ETuUafWQHbW5SD8xw99bFgBXlaw6qrP15/5YNTpynLpcuW6F6PA140d0iBK0wto0idnm2KV/VvOMQrK4ONHUjg==

# Application will automatically detect PostgreSQL from connection string pattern
# and configure Npgsql Entity Framework provider